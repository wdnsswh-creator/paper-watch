name: AI Thesis Draft

on:
  workflow_dispatch:
    inputs:
      openai_model:
        description: "OpenAI model used to generate the thesis draft"
        required: true
        default: "gpt-5"
        type: string

permissions:
  contents: write

jobs:
  ai-thesis-draft:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install pandas openpyxl openai

      - name: Generate AI thesis draft
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ inputs.openai_model }}
        run: |
          python scripts/generate_thesis_draft.py

      - name: Commit AI draft outputs
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add outputs/
          git commit -m "Generate AI thesis draft" || echo "No changes to commit"
          git push
